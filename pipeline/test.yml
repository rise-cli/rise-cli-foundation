version: 0.2
phases:
    install:
        runtime-versions:
            nodejs: 14

    build:
        commands:
            - cd ./appV2
            - npm i -g ts-node
            - npm i
            - npm run test
